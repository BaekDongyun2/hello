#include <stdio.h>

int main() {

	/*
	int num1 = 5;

	if (num1 == 10)
	{
		printf("10입니다.\n"); // 조건식이 만족할 때 실행할 코드 (참)
	}
	else
	{
		printf("10이 아닙니다.\n"); // 조건식이 만족하지 않을 때 실행할 코드 (거짓)
	}


	int num2 = 10;

	if (num2 == 10)
	{
		printf("10입니다.\n");
	}
	else; // else에 세미콜론을 붙이면 안 됨
	{
		printf("10이 아닙니다.\n"); // else에 세미콜론을 붙였으므로, if의 결과와는 관계 없이 항상 실행됨
	}

	// else도 if와 마찬가지로 실행할 코드가 한 줄이면 중괄호를 생략할 수 있다.

	int num3 = 10;

	if (num3 == 10)
		printf("if 조건문\n");
	    printf("10입니다.\n");
	else // 컴파일 에러 발생
		printf("else\ n");
		printf("10이 아닙니다.\n"); 
	*/

	return 0;
}