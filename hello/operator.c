#include <stdio.h>

int main()
{
	/*
	// AND
	// 논리 연산에서 중요한 부분은 단락 평가(short-circuit evaluation)이다.
	// 단락 평가는 첫 번째 값만으로 결과가 확실할 때 두 번째 값은 확인(평가)하지 않는 방법을 말한다.
	// 즉, AND연산자는 두 값이 모두 참이라야 참이므로 첫 번째 값이 거짓이면 두 번째 값은 확인하지 않고 바로 거짓으로 결정한다.
	printf("%d\n", 1 && 1); // 두 값이 참이기 때문에 1 출력
	printf("%d\n", 1 && 0); // 0 출력
	printf("%d\n", 0 && 1); // 0 출력
	printf("%d\n", 0 && 0); // 0 출력
	printf("%d\n", 2 && 3); // 두 값이 참이기 때문에 1 출력
	
	// OR <-- 둘 중에 하나라도 1(참)이면 참이다
	printf("%d\n", 1 || 1);
	printf("%d\n", 1 || 0);
	printf("%d\n", 0 || 1);
	printf("%d\n", 0 || 0);
	printf("%d\n", 2 || 3);

	// NOT
	printf("%d\n", !1);
	printf("%d\n", !0);

	printf("%d\n", !3);

	int num1 = 20;
	int num2 = 10;
	int num3 = 30;
	int num4 = 15;

	printf("%d\n", num1 > num2 && num3 > num4);
	printf("%d\n", num1 > num2 && num3 < num4);

	printf("%d\n", num1 > num2 || num3 < num4);
	printf("%d\n", num1 < num2 || num3 < num4);
	printf("%d\n", !(num1 > num2));

	int num1 = 1;
	int num2 = 0;

	
	// 조건문에서 논리 연산자 사용
	if (num1 && num2)
		printf("참\n");
	else
		printf("거짓\n");
	if (num1 || num2)
		printf("참\n");
	else
		printf("거짓\n");
	if (!num1)
		printf("참\n");
	else
		printf("거짓\n");

	// 삼항 연사자에서 논리 연산자 사용
	int num1 = 1;
	int num2 = 0;

	printf("%s\n", num1 && num2 ? "참" : "거짓"); // 거짓: 삼항 연산자에서 AND 연산자 사용
	printf("%s\n", num1 || num2 ? "참" : "거짓"); // 참: 삼항 연산자에서 OR 연산자 사용
	*/



	return 0;
}