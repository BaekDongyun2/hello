#include <stdio.h>
#include <stdbool.h> // bool, true, false가 정의된 헤더 파일

int main()
{
	/*
	bool b1 = true;

	if (b1 == true)
		printf("참\n");
	else
		printf("거짓\n");

	printf("int의 크기: %d\n", sizeof(int)); // int의 크기: 4byte
	printf("bool의 크기: %d\n", sizeof(bool)); // bool의 크기: 1byte

	// 불 자료형과 논리 연산자 사용
	// printf로 결과를 출력할 때는 정수를 출력하는 것처럼 서식 지정자로 %d를 사용한다.
	// stdbool.h에넌 자료형과 불값만 정의할 뿐 전용 서식 지정자는 없다.
	printf("%d\n", true && true);
	printf("%d\n", true && false);
	printf("%d\n", false && true);
	printf("%d\n", false && false);

	printf("%d\n", true || true);
	printf("%d\n", true || false);
	printf("%d\n", false || true);
	printf("%d\n", false || false);
	
	// true, false를 문자열로 출력
	bool b1 = true;
	bool b2 = false;

	printf(b1 ? "true" : "false");
	printf("\n");
	printf(b2 ? "true" : "false");
	printf("\n");

	printf("%s\n", b1 ? "true" : "false");
	printf("%s\n", b2 ? "true" : "false");

	// if 조건문에서 불 자료형 사용

	if (true)
		printf("참\n");
	else
		printf("거짓\n");
	if (false)
		printf("참\n");
	else
		printf("거짓\n");
	*/


	
	return 0;
}